name: GitHub Actions Test
run-name: Github actions test
on:
  schedule:
    - cron: "52 1 * * *"
      timezone: "America/New_York"

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test"
