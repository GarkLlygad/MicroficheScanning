on:
  schedule:
    - cron: '42 5 * * *'
    timezone: "America/Los_Angeles"

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Every time
        run: echo "This step will always run"
